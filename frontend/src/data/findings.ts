// Types only — no mock data.
// All types are defined in src/lib/api.ts
// This file is kept for convenience re-exports so import paths stay consistent.
export type { Severity, Status, Finding } from "../lib/api";
